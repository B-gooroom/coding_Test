function solution(numbers) {
    // var answer = 0;
    let total = 0;
    for(let i = 0; i <= 9; i ++) {
        numbers.indexOf(i);
        if (numbers.indexOf(i) === -1) {
            total += i
        }
        // console.log(numbers.indexOf(i))
    }
    console.log(total)
    return total;
}
